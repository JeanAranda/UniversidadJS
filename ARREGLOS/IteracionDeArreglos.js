//Iterar arreglos
//Declarar arreglos
let numerosArreglo = [13, 21, 12, 34, "Hola"]
//Leer los valores
console.log(numerosArreglo.length)
for  (let i = 0; i < numerosArreglo.length; i++){
    console.log(`Arreglo[${i}] = ${numerosArreglo[i]}`)
}